name: TEMP Supabase builder catch

on:
  push:
    branches: [fix/supabase-builder-catch]

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test tests/taphoa-direct-sheet-mutations.test.js
